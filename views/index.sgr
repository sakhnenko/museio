extends(src='layout.sgr')
  block(name='content')
    h2 Citie
    ul
      each(loop='city of contentful.cities')
        li {{city.fields.cityName}} 
          ul
            each(loop='museum of contentful.museums')
              li {{museum.fields.name}}
